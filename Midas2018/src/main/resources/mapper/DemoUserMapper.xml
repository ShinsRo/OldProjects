<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.midas2018.root.repository.DemoUserMapper">

  <select id="selectUser" parameterType="map" resultType="com.midas2018.root.model.User">
    SELECT
      id
      ,name
      ,password
    FROM
      user
    WHERE
      id = #{id};
  </select>


</mapper>
