import requests
import sys
from src.modules.scraper import Scraper

results = []

# print(results)

# path = "http://apps.webofknowledge.com"
# res = requests.get("http://apps.webofknowledge.com")
# jsessionid = res.headers['Set-Cookie'].split('=')[1].split('; ')[0]
# print(jsessionid)

# path += "/home.do"
# path += ";jessionid=" + jsessionid

# res = requests.get(path)
# loc = res.headers['Location']

print(loc)