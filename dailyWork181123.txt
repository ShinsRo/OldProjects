18.11.23
[기술]
 - [112201해결] cmd를 App.vue의 상위 컴포넌트에서 선언
 - [112201해결] cmd에서 props로 cmd 프로세스를 전달
 - [112201해결] 하위 컴포넌트에서 emit으로 이벤트를 발생 -> 상위에서 핸들링
    - [112301이슈] 상위 컴포넌트에 등록한 이벤트 핸들러가 cmd에 접근하지 못함.......아...
    - [112301해결] 하위 컴포넌트에서 emit으로 이벤트를 발생하고, 페이로드에 스코프를 전달

 - App.vue에 진행 로그 컴포넌트를 글로벌하게 사용할 수 있도록 함

 - dispatcher 모듈을 통한 UI - python core 통신 개선
    - dispatcher를 통해 모든 stdin를 한 통로로 받는 작업
    - dispatcher에서 stdin으로 받는 명령을 분기하는 작업
    - dispatcher에서 모든 서비스를 스레딩 초기화하는 작업
        - [112302이슈] MutiCitationSearch 서비스가 내부적으로 SingleCitationSearch를 사용하면서 
        로그가 겹치는 문제
        - [112302해결] Future 객체를 통해 병렬 초기화 및 동기화.
    
 - [112303이슈] 뷰 전환 시 프로세스 stdin 스트림이 파괴되는 이슈 발생 (EOF 에러)
    - [112303해결] 최초 설계 시 vue Destroy Cycle에서 스트림을 끊는 작업을 해놓은 바 이슈가 발생
    - [112303해결] citationSearchView의 Destroy Cycle에서 해당 내용을 등록 해제

 - 각종 버그 수정
    - WOS에 impact_factor, JCR 등이 존재하지 않는 경우에 대한 에러 처리

 - 브라우저 초기화 루틴을 함수화, 코드 축약
 - 인용중 논문 정보 xls 저장에서 WOS 서버 오류 방어
 - 인용중 논문 정보 xls 제목을 타이틀에서 아이디로 변경, 뷰에 적용.

 - commonSearchMultiView 추가
 - 좌측 네이게이션 수정

[서류]
 - 12시 경 클라리베이트에서 진행 현황을 제공, 아직 계약이 완료되지 않는 것으로 보임
 - 새로운 API 명세를 제공하면서 프리미엄 필드가 선택적으로 제공되지 않는 것으로 수정 공지
 - API 프리미엄 견적을 재요청