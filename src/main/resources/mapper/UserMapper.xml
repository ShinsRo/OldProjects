<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.midas2018.root.repository.UserMapper">
  <sql id="UserVOColumns">
          user.id
        ,	user.name
        , user.email
        ,	user.created_time
        ,	user.updated_time
        ,	user.deleted_time
        ,	user.department
        ,	user.sub_department
        ,	user.phone_number
        ,	user.status
      </sql>

  <insert id="userRegister" parameterType="com.midas2018.root.model.User">
        INSERT INTO
        user(email, password, name)
        value(#{email}, #{password}, #{name})
    </insert>

  <select id="selectUserStatusByUserId" parameterType="map" resultType="com.midas2018.root.model.UserStatus">
        SELECT
          `status`
        FROM
          user
        WHERE
          id = #{id}
    </select>

  <select id="signin" parameterType="map" resultType="com.midas2018.root.model.UserVO">
        SELECT
          <include refid="UserVOColumns" />
        FROM
          user
        WHERE
          email = #{email}
        AND password = #{password}
    </select>
</mapper>
