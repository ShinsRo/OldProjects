<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.midas2018.root.repository.UserOrderMapper">
       <sql id="UserOrderVOColumns">
          user_order.id
        ,	user_order.cafe_menu_list
        , user_order.total_price
        ,	user_order.content
        ,	user_order.status
        ,	user_order.deleted_time
        ,	user_order.created_time
        ,	user_order.updated_time
      </sql>
  <select id="selectUserOrderList" resultType="com.midas2018.root.model.UserOrderVO">
    SELECT
      <include refid="UserOrderVOColumns" />
    FROM
      user_order
  </select>

  <insert id="insertUserOrder" parameterType="map">
    INSERT INTO
      user_order(
        cafe_menu_list
        , total_price
        , content
        , status
        , created_time
        , updated_time
      )
      VALUES(
        #{cafeMenuList}
        , #{totalPrice}
        , #{content}
        , #{status}
        , NOW()
        , NOW()
      )
  </insert>


</mapper>
